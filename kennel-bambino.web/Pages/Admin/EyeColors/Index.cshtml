@page
@model kennel_bambino.web.Pages.Admin.EyeColors.IndexModel
@{

    ViewBag.Title = "EyeColor List";
}

<section>

    <h1 class="text-capitalize display-4"><i class="fas fa-eye fa-fw text-right"></i>  @ViewBag.Title</h1>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-dismissible alert-success">
            <i class="fa fa-check fa-fw"></i> @TempData["Success"]
        </div>
    }

    <div class="clearfix">
        <a asp-page="Create" class="btn btn-primary text-capitalize float-left mb-3">
            <i class="fa fa-plus fa-fw"></i> AddNew
        </a>

        <button type="button" class="btn btn-primary text-capitalize float-right mb-3">
            <i class="fa fa-th-list fa-fw"></i> @Model.EyeColorsCount
        </button>

    </div>

    @if (Model.EyeColors.Any())
    {
        <div class="table-responsive">
            <table class="table table-hover table-striped table-hover">
                <thead>
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col">Title</th>
                        <th scope="col">Operation</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var eyecolor in Model.EyeColors)
                    {
                        <tr>
                            <td>@eyecolor.EyeColorId</td>
                            <td>@eyecolor.Name</td>
                            <td>
                                <a asp-page="Edit" asp-route-id="@eyecolor.EyeColorId"
                                   class="btn btn-outline-warning btn-sm"><i class="fa fa-pen fa-fw"></i>Edit</a>

                                <a asp-page="Delete" asp-route-id="@eyecolor.EyeColorId"
                                   class="btn btn-sm btn-outline-danger"><i class="fa fa-trash-alt fa-fw"></i>Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="alert alert-dismissible border border-warning alert-danger">
            <p class="mb-2">
                <i class="fa fa-exclamation-triangle fa-fw"></i> <strong>No</strong> EyeColor Exist.
            </p>
        </div>
    }


</section>

